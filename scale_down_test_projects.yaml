---
 - hosts: AWX
   become_user: azureuser
   tasks:
    - name: Scale Down test projects
      shell: ./scale_down_ent64.sh
      register: output

    - name: Print Scaled deployment
      debug:
       msg: "{{output }}"
       
    - name: Scale Down qe projects
      shell: ./scale_down_mainline.sh
      register: output

    - name: Print Scaled deployment
      debug:
       msg: "{{output }}"
