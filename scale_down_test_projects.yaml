---
 - hosts: AWX
   become_user: azureuser
   tasks:
    - name: Scale Down test projects
      shell: ./scale_down_ent64.sh
      register: output

    - name: Print Results Tickets
      debug:
       msg: "{{output }}"
