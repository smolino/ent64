---
 - hosts: AWX
   become_user: "{{ username }}"
   tasks:
   
    - name: Select working dir
      command: cd /home/{{ username }}
   
    - name: Start cypress test
      command: kubectl exec -ti cypress -n cypress -- npm run cypress:smoke -- --config-file configs/ent64azure/remoteconfig.json
      register: output
