---
 - hosts: AWX
   become_user: azureuser
   tasks:
   
   - name: Get url
     shell: kubectl get svc -n ingress-nginx |grep LoadBalancer | awk '{print $4}' | while read HOST;do 
            curl "http://$HOST/auth/";done

   - name: Get url
     command: kubectl get svc -n ingress-nginx |grep LoadBalancer | awk '{print $4}' | while read HOST;do 
            application="http://$HOST/app-builder/";done            

#   - name: Curl website
#     uri:
#      url: "{{ entando_application_var }}"
#      return_content: yes
#     register: webpage
     
   - name: Curl website
     command: curl $application
     register: webpage
   - debug: var=webpage.stdout
   - name: Fail
     fail:
     when: "'entando' not in webpage.stdout" 
   - name: Success
     debug: msg="Congrats Yours HW Assignment is completed"
